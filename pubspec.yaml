name: healix
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish this package.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  cupertino_icons: ^1.0.8
  # Add the image picker for profile image functionality

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Assets for profile image or other media
 
    # Optional: If you have a default image stored in the assets folder
